# รายการงานสำหรับการปรับโฉมอินเตอร์เฟสผู้ดูแลระบบและกล่องรูปภาพ (Tasks)

- `[ ]` ปรับปรุงความโค้งมนของรูปภาพประกอบในส่วนหน้าหลักและแถบเครื่องเล่นให้เป็น `rounded` ธรรมดา:
  - `[ ]` ปรับปรุง `components/song-item.tsx`
  - `[ ]` ปรับปรุง `components/player-view.tsx`
  - `[ ]` ปรับปรุง `components/request-view.tsx`
  - `[ ]` ปรับปรุง `components/player-bottom-bar.tsx`
- `[ ]` ปรับแต่งสไตล์สว่าง (Light Theme) ใน `globals.css` สำหรับผู้ดูแลระบบ
- `[ ]` ปรับปรุงอินเตอร์เฟสผู้ดูแลระบบ `/admin` ใน `admin-view.tsx` ใหม่ทั้งหมด:
  - `[ ]` ยกเลิกการล็อกความกว้างหน้าจอเพื่อให้ทำงานแบบเต็มความกว้าง (Full Screen Width)
  - `[ ]` ติดตั้งระบบยืดหดและปรับความกว้างแถบข้างด้านซ้ายได้ (Resizable & Toggleable Sidebar)
  - `[ ]` ปรับมุมมองการ์ดเพลย์ลิสต์ให้เรียงตัวเป็นตารางรูปหน้าปก (Thumbnail Grid View) คล้ายคลึงกับ Spotify
  - `[ ]` ปรับขนาดความสูงกล่องรายการเพลงและผลลัพธ์การค้นหาให้ขยายตัวเต็มขนาดหน้าจอ (Full Div Height)
  - `[ ]` ติดตั้งสวิตช์เลือกสลับมุมมอง "รายการ (List)" และ "ตาราง (Grid Thumbnail)" สำหรับแทร็กเพลงใน Workspace
- `[ ]` รันตรวจสอบโปรเจกต์ Next.js (Build & TS Compilation Check)
- `[ ]` บันทึกรายละเอียดประวัติลงใน `CHANGELOG.md`
- `[ ]` ดำเนินการบันทึก Git commit รหัสต้นฉบับ
