# รายการงานสำหรับการปรับปรุงอินเตอร์เฟสผู้ดูแลระบบและเพลย์ลิสต์ (Tasks)

- `[x]` ปรับปรุงโมเดลประเภทข้อมูลใน `types.ts` (เพิ่มฟิลด์ `cover_thumbnail`)
- `[x]` อัปเดต SQL Query ใน `/api/playlists/route.ts` เพื่อส่งกลับรูปภาพหน้าปกของแต่ละเพลย์ลิสต์
- `[x]` ปรับแต่งสไตล์ระบบใน `globals.css` ให้หน้าควบคุมผู้ดูแลระบบเป็นสไตล์ Premium Dark Glassmorphism หรูหรา
- `[x]` ปรับปรุงหน้าจอผู้ดูแลระบบ `/admin` ใน `admin-view.tsx` ใหม่ทั้งหมด:
  - `[x]` ออกแบบรูปแบบ Console แบ่ง 2 ฝั่ง (Split Panel Layout) สำหรับหน้าจอใหญ่ และรักษารูปแบบเรียบร้อยสำหรับอุปกรณ์พกพา
  - `[x]` แสดงรูปภาพเพลย์ลิสต์ (`cover_thumbnail`) ร่วมกับหน้าปกไล่โทนสีอัตโนมัติ (Elegant CSS Gradient Art) เมื่อไม่มีเพลงในเพลย์ลิสต์
  - `[x]` แก้ไขปัญหาการยกเลิกการเลือกเพลย์ลิสต์ โดยอนุญาตให้ส่งชุดว่างเพื่อล้างการตั้งค่ากลับเป็นหลักได้สำเร็จ
- `[x]` ปรับแก้แถบควบคุมเพลงด้านล่าง `player-bottom-bar.tsx` ให้แสดงหน้าปกเพลย์ลิสต์ และรองรับการกดยกเลิกการเลือก (Deselect / Toggle Off) เมื่อกดซ้ำตัวเดิม
- `[x]` ตรวจสอบและแก้ไขข้อผิดพลาดระบบ (Next.js Build & Lint Testing)
- `[x]` อัปเดตประวัติการพัฒนาลงในไฟล์ `CHANGELOG.md`
- `[ ]` ทำการ Git commit รหัสต้นฉบับเวอร์ชันล่าสุด
